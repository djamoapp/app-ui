import 'package:flutter/material.dart';

class AppDuoCTA extends StatelessWidget {
  const AppDuoCTA({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
